#include <stdio.h>

int main()
{
    int number;
    printf("10 to 1 Numbers is: ");
    for (number = 10; number >= 1; number--)
    {
        printf("%d \t", number);
    }
    return 0;
}