#include <stdio.h>

int main(){
    int number, square;
    printf("Enter The Number: ");
    scanf("%d", &number);
    square = number * number;
    printf ("The Square is %d", square);
    return 0;
}