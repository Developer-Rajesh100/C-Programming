// This Program is Wrong
/*
#include <stdio.h>

int main()
{
    float height, down, ans;

    printf("Enter The Height And Down Side Length : ");
    scanf("%f %f", &height, &down);

    ans = (float)1 / 2 * height * down;

    printf("The Answer is : %.2f", ans);

    return 0;
}
*/

// OR

/*
#include <stdio.h>

int main()
{
    float a, b, c, s, area;

    printf("Enter The Values : ");
    scanf("%f %f %f", &a, &b, &c);

    s = (a + b + c) / 2;
    area = sqrt(s * (s - a) * (s - b) * (s - c));

    printf("The Answer is : %.2f", area);

    return 0;
}

*/
