// Variable Storage

#include <stdio.h>>

int main()
{
    int a;
    float f;
    double d;
    char c;

    printf("int : %d, \nfloat : %d, \ndouble : %d, \nchar : %d,", sizeof(a), sizeof(f), sizeof(d), sizeof(c));

    return 0;
}